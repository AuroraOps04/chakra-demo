import { Box, HStack, Stack } from '@chakra-ui/react'

const Header = () => {
  return (
    <Stack bg="pink" h={60}>
      <HStack>
        <Box>this is header</Box>
      </HStack>
      {/* tag-view */}
    </Stack>
  )
}

export default Header
